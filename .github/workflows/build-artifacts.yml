name: Build Artifacts

on:
  workflow_dispatch:

  # Build main only when you create a release tag like v1.0.0
  push:
    tags:
      - "v*"

  # Build nightly branch automatically every night
  schedule:
    - cron: "17 9 * * *"

env:
  CARGO_TERM_COLOR: always
  APP_NAME: CrystalBall

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    # Scheduled builds should only build nightly.
    # Tag builds build releases from main/tag.
    if: github.event_name != 'schedule' || github.ref == 'refs/heads/nightly'

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux
            os: ubuntu-latest
            binary: CrystalBall
            archive: CrystalBall-linux-x86_64.tar.gz
          - name: macos
            os: macos-latest
            binary: CrystalBall
            archive: CrystalBall-macos-app.tar.gz
          - name: windows
            os: windows-latest
            binary: CrystalBall.exe
            archive: CrystalBall-windows-x86_64.zip

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
          ref: ${{ github.event_name == 'schedule' && 'nightly' || github.ref }}

      - name: Install Linux GUI dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libxkbcommon-dev

      - name: Build release binary
        run: cargo build --locked --release

      - name: Stage Linux artifact
        if: runner.os == 'Linux'
        shell: bash
        run: |
          mkdir -p dist/${{ matrix.name }}
          cp target/release/${{ matrix.binary }} dist/${{ matrix.name }}/
          cp README.md dist/${{ matrix.name }}/
          cat > dist/${{ matrix.name }}/install.sh <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail
          mkdir -p "$HOME/.local/bin"
          cp CrystalBall "$HOME/.local/bin/CrystalBall"
          chmod +x "$HOME/.local/bin/CrystalBall"
          echo "Installed CrystalBall to $HOME/.local/bin/CrystalBall"
          EOF
          chmod +x dist/${{ matrix.name }}/install.sh

      - name: Stage macOS app bundle
        if: runner.os == 'macOS'
        shell: bash
        run: |
          scripts/package-macos.sh target/release/${{ matrix.binary }} dist/${{ matrix.name }}
          cp README.md dist/${{ matrix.name }}/
          cat > dist/${{ matrix.name }}/install.sh <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail

          APP_NAME="CrystalBall.app"
          INSTALL_DIR="${1:-/Applications}"
          SOURCE_APP="$(pwd)/$APP_NAME"

          if [[ ! -d "$INSTALL_DIR" || ! -w "$INSTALL_DIR" ]]; then
            INSTALL_DIR="$HOME/Applications"
          fi

          mkdir -p "$INSTALL_DIR"
          rm -rf "$INSTALL_DIR/$APP_NAME.new"
          cp -R "$SOURCE_APP" "$INSTALL_DIR/$APP_NAME.new"
          rm -rf "$INSTALL_DIR/$APP_NAME"
          mv "$INSTALL_DIR/$APP_NAME.new" "$INSTALL_DIR/$APP_NAME"
          xattr -dr com.apple.quarantine "$INSTALL_DIR/$APP_NAME" 2>/dev/null || true

          echo "Installed CrystalBall to $INSTALL_DIR/$APP_NAME"
          EOF
          chmod +x dist/${{ matrix.name }}/install.sh

      - name: Stage Windows artifact
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist/${{ matrix.name }} | Out-Null
          Copy-Item target/release/${{ matrix.binary }} dist/${{ matrix.name }}/
          Copy-Item README.md dist/${{ matrix.name }}/
          @'
          $PidToWait = $args[0]
          $InstallDir = "$env:LOCALAPPDATA\CrystalBall"
          $Source = (Resolve-Path ".\CrystalBall.exe").Path
          $Target = Join-Path $InstallDir "CrystalBall.exe"
          New-Item -ItemType Directory -Force -Path $InstallDir | Out-Null

          try {
            Copy-Item $Source $Target -Force -ErrorAction Stop
            Write-Host "Installed CrystalBall to $Target"
          } catch {
            if (-not $PidToWait) {
              throw
            }

            $Helper = Join-Path $env:TEMP "crystalball-finish-update.ps1"
            @"
          `$PidToWait = $PidToWait
          `$Source = '$Source'
          `$Target = '$Target'
          while (Get-Process -Id `$PidToWait -ErrorAction SilentlyContinue) {
            Start-Sleep -Milliseconds 500
          }
          Copy-Item `$Source `$Target -Force
          Start-Process `$Target
          "@ | Set-Content -Path $Helper

            Start-Process powershell -WindowStyle Hidden -ArgumentList "-NoProfile", "-ExecutionPolicy", "Bypass", "-File", $Helper
            Write-Host "CrystalBall update will finish after the app closes."
          }
          '@ | Set-Content -Path dist/${{ matrix.name }}/install.ps1

      - name: Package artifact
        if: runner.os != 'Windows'
        shell: bash
        run: |
          tar -C dist/${{ matrix.name }} -czf dist/${{ matrix.archive }} .

      - name: Package artifact
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive -Path dist/${{ matrix.name }}/* -DestinationPath dist/${{ matrix.archive }}

      - name: Upload downloadable build
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.archive }}
          path: dist/${{ matrix.archive }}
          if-no-files-found: error

      - name: Attach artifacts to GitHub release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/${{ matrix.archive }}
